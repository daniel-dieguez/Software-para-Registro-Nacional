package com.sistema.dao.repository;

import com.sistema.modals.modal.Region;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IRegionDao extends JpaRepository<Region, Object> {
}
